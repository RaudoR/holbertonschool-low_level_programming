#include "holberton.h"
/**
 * print_sign - main function
 * @n: integer
 * Return: 0
 */
int print_sign(int n);

{
	if (n > 0)
	{
		_putchar('+');
		return (1);
	}
	else if (c == 0)
	{
		_putchar(0);
		return (0);
	}
	else if (c < 0)
	{
		_putchar(-1);
		return (-1);
	}
}
